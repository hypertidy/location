% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loc_tz.R
\name{loc_tz}
\alias{loc_tz}
\title{Look up the timezone for a place name}
\usage{
loc_tz(x, method = "fast", ...)
}
\arguments{
\item{x}{character vector of place names.}

\item{method}{passed to \code{\link[lutz:tz_lookup_coords]{lutz::tz_lookup_coords()}}: \code{"fast"} (default,
Rcpp-based, no sf dependency) or \code{"accurate"} (sf polygon intersection).}

\item{...}{passed to \code{\link[=loc]{loc()}}.}
}
\value{
A character vector of IANA timezone names (e.g.
\code{"America/Chicago"}), the same length as \code{x}. \code{NA} where geocoding or
timezone lookup failed.
}
\description{
A convenience wrapper that geocodes a place name with \code{\link[=loc]{loc()}} and then
looks up the IANA timezone via the \pkg{lutz} package.
}
\examples{
\dontrun{
loc_tz("Jackson, TN")
loc_tz("Davis Station, Antarctica")
loc_tz(c("Hobart", "McMurdo"))
}

}
